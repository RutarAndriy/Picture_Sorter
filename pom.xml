<?xml version="1.0" encoding="UTF-8"?>

<!-- Generated by MavenOwnArchetype_JavaSE v1.0.0 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rutar</groupId>
    <artifactId>Picture_Sorter</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Picture_Sorter</name>
    <url>https://github.com/RutarAndriy/Picture_Sorter</url>

    <developers>
        <developer>
            <id>rutar</id>
            <name>Rutar Andriy</name>
            <email>RutarAndriy@gmail.com</email>
        </developer>
    </developers>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <app.mainClass>com.rutar.picture_sorter.Picture_Sorter</app.mainClass>
    </properties>
    
    <repositories>

        <!-- Репозиторій JitPack.IO -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>

    </repositories>

    <dependencies>
        
        <!-- Look and Feel -->
        <dependency>
	    <groupId>com.github.RutarAndriy</groupId>
	    <artifactId>Custom_Flat_LaF</artifactId>
	    <version>v1.0.0</version>
	</dependency>
        
        <!-- Бібліотека для відображення діалогових вікон -->
        <dependency>
            <groupId>com.github.RutarAndriy</groupId>
            <artifactId>JMessenger</artifactId>
            <version>v1.0.0</version>
        </dependency>
        
        <!-- Бібліотека для інтеграції DaD-панелей -->
        <dependency>
	    <groupId>com.github.RutarAndriy</groupId>
	    <artifactId>JDroppablePanel</artifactId>
	    <version>v1.0.0</version>
	</dependency>
        
        <!-- Українська локалізація елементів інтерфейсу -->
        <dependency>
	    <groupId>com.github.RutarAndriy</groupId>
	    <artifactId>UA_Translator</artifactId>
	    <version>v1.1.0</version>
	</dependency>
        
        <!-- JUnit тести -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        
    </dependencies>

    <build>
        <plugins>
            
            <!-- Збирання jar-файлу -->
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>false</addClasspath>
                            <mainClass>${app.mainClass}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!-- Збирання jar-файлу із залежностями -->
            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <minimizeJar>true</minimizeJar>
                    <shadedArtifactAttached>true</shadedArtifactAttached>
                    <shadedClassifierName>jar-with-dependencies</shadedClassifierName>
                    <createDependencyReducedPom>false</createDependencyReducedPom>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>${app.mainClass}</mainClass>
                        </transformer>
                    </transformers>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/maven/**</exclude>
                                <exclude>META-INF/*.txt</exclude>
                            </excludes>
                        </filter>
                        <!--
                        <filter>
                            <artifact>[groupId]:[artifactId]</artifact>
                            <includes>
                                <include>com/rutar/example/**</include>
                            </includes>
                            <excludes>
                                <exclude>com/rutar/example/Example*</exclude>
                            </excludes>
                        </filter>
                        -->
                    </filters>
                </configuration>
                <executions>
                    <execution>
                        <id>make-shade</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
</project>
